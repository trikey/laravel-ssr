# Laravel SSR template

## Requirements

v8 engine:

`brew install v8`

`sudo pecl install v8js`

## Installation

`composer install`

`npm install`
    
`php artisan key:generate`
    
## Description

